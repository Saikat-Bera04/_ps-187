import swaggerJsdoc from 'swagger-jsdoc';

const options: swaggerJsdoc.Options = {
  definition: {
    openapi: '3.0.0',
    info: {
      title: 'IBVAP Backend API',
      version: '1.0.0',
      description: 'API documentation for Intelligent Border Video Analytics Platform',
    },
    components: {
      securitySchemes: {
        bearerAuth: {
          type: 'http',
          scheme: 'bearer',
          bearerFormat: 'JWT',
        },
      },
    },
    security: [{ bearerAuth: [] }],
  },
  apis: ['./src/routes/*.ts'], // In a real scenario we would document each endpoint via comments
};

export const swaggerSpec = swaggerJsdoc(options);
